services:
  app:
    build: .
    container_name: booking-app
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - default

networks:
  default:
    external: true
    name: sky_default
