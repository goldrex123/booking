services:
  app:
    build: .
    container_name: booking-app
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    env_file:
      - .env
    restart: unless-stopped
    ports:
        - '8080:8080'
    networks:
      - default

networks:
  default:
    external: true
    name: sky_default
